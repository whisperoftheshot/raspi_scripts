Some scripts I created to install my most used packages:
shairport
owncloud
octoprint
and all the prereq's

Basic Setup:
cd ~
git clone https://github.com/whisperoftheshot/raspi_scripts
cd raspi_scripts
chmod a+x raspi.sh
sudo ./raspi.sh